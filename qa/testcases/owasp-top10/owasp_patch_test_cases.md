# SentinelAI OWASP Top 10 (2021): Patch Verification Test Cases

| Test Case ID | Category & File | Preconditions | Steps | Expected Result (Patch Review) |
| :--- | :--- | :--- | :--- | :--- |
| **TC_OWASP_001** | **A01: Broken Access Control**<br>`broken_access_control_node.js` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the `/admin/delete-user` route.<br>3. Apply patch. | **Security Mechanism:** The AI must propose adding an authorization middleware (e.g., `verifyAdmin`) or an explicit inline role check (e.g., `if (req.user.role !== 'admin')`) before executing the `deleteUserFromDatabase` function to prevent unauthorized access. |
| **TC_OWASP_002** | **A02: Cryptographic Failures**<br>`crypto_failure_node.js` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the password hashing logic.<br>3. Apply patch. | **Security Mechanism:** The AI must propose replacing the weak `MD5` hashing algorithm with a strong, adaptive hashing function like `bcrypt` or `argon2`, including the generation and usage of a secure salt. |
| **TC_OWASP_003** | **A03: Injection**<br>`sqli_python.py` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the SQLite query.<br>3. Apply patch. | **Security Mechanism:** The AI must propose using parameterized queries (e.g., `execute("SELECT * FROM users WHERE username = ?", (username,))`) to strictly separate user input from the SQL structure. |
| **TC_OWASP_004** | **A04: Insecure Design**<br>`insecure_design_python.py` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the checkout logic.<br>3. Apply patch. | **Security Mechanism:** The AI must propose fetching the authoritative price from a trusted server-side source (e.g., a secure database lookup using `item_id`) rather than blindly trusting the `client_provided_price` submitted from the client. |
| **TC_OWASP_005** | **A05: Security Misconfiguration**<br>`security_misconfig_node.js` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the error handler.<br>3. Apply patch. | **Security Mechanism:** The AI must propose suppressing the full `err.stack` from being exposed to the client, replacing it with a generic error message (e.g., `res.status(500).send('Internal Server Error')`) while securely logging the full stack trace server-side. |
| **TC_OWASP_006** | **A06: Vulnerable and Outdated Components (XSS context)**<br>`xss_javascript.js` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for `innerHTML`.<br>3. Apply patch. | **Security Mechanism:** The AI must propose applying strict DOM sanitization (e.g., using `DOMPurify.sanitize()`) before assigning the user input to `innerHTML`, or switching to a safe property like `textContent`. |
| **TC_OWASP_007** | **A07: Identification and Authentication Failures**<br>`auth_failure_node.js` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the JWT signing.<br>3. Apply patch. | **Security Mechanism:** The AI must propose moving the hardcoded JWT secret to an environment variable (e.g., `process.env.JWT_SECRET`) to prevent unauthorized and verifiable token generation. |
| **TC_OWASP_008** | **A08: Software and Data Integrity Failures**<br>`insecure_deserialization_python.py` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for `pickle.loads`.<br>3. Apply patch. | **Security Mechanism:** The AI must propose replacing the dangerous `pickle` module with a safe data format/parser like `json` (e.g., `json.loads()`) for deserializing untrusted user session data. |
| **TC_OWASP_009** | **A09: Security Logging and Monitoring Failures (XXE context)**<br>`xxe_python.py` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for the XML parser.<br>3. Apply patch. | **Security Mechanism:** The AI must propose disabling external entity resolution (e.g., setting `resolve_entities=False` in `lxml`) or migrating to a securely configured alternative like the `defusedxml` library. |
| **TC_OWASP_010** | **A10: Server-Side Request Forgery (SSRF)**<br>`ssrf_python.py` | 1. SentinelAI active.<br>2. File is open. | 1. Scan the file.<br>2. Review proposed fix for `requests.get`.<br>3. Apply patch. | **Security Mechanism:** The AI must propose validating the `image_url` against a strict allowlist of permitted hostnames, domains, or IP addresses before initiating the server-side HTTP request. |
