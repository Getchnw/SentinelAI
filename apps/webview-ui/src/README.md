# apps/webview-ui/src

Source code for the webview UI.

## Subfolders
- `components/`: reusable UI components such as chat panels, diff views, buttons, and status cards.
- `styles/`: CSS and theme tokens for the webview.

## Entry files
- `main.tsx`: React bootstrap.
- `App.tsx`: top-level UI composition.
